
<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <!-- Meta, title, CSS, favicons, etc. -->
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>SINJUR</title> 
  </head>

  <body class="login">
    <div> 
    
      <div class="login_wrapper">
        <div class="animate form login_form">
          <section class="login_content"> 
              <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                <h1>Login</h1> 
                <div class="field">
                  <%= f.label :email %><br />
                  <%= f.email_field :email, autofocus: true, class:"form-control", placeholder:"Emails"%>
                </div> 
                <div class="field">
                  <%= f.label :password %><br />
                  <%= f.password_field :password,type:"password", class:"form-control", placeholder:"Senha"%>
                </div>
                <% if devise_mapping.rememberable? %>
                  <div class="field">
                    <%= f.check_box :remember_me %>
                    <%= f.label :remember_me %>
                  </div>
                <% end %> 
                <div class="actions"> 
                  <a class="btn btn-default submit"><%= f.submit "Login"%></a>
                </div>
              <% end %>
              <div class="clearfix"></div>
              <% flash.each do |key, value|%>
              <div class="alert alert-success">
                <div class="flash <%=key%>"><%= value %></div>
              </div>
              <% end %>
              <div class="separator">
                 
                <div class="clearfix"></div>
                <br />

                <div>
                  <h1>SinJur</h1>
                  <p>©2020 Todos os direitos reservados a Sinoredi-Ce</p>
                </div>
              </div> 
          </section>
        </div>
      </div>
    </div>
  </body>
</html>
