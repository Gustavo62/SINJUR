<%= form_with(model: tabeliao, local: true) do |form| %>
  <% if tabeliao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tabeliao.errors.count, "error") %> prohibited this tabeliao from being saved:</h2>

      <ul>
      <% tabeliao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :cpf %>
    <%= form.number_field :cpf %>
  </div>

  <div class="field">
    <%= form.label :numero %>
    <%= form.number_field :numero %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
